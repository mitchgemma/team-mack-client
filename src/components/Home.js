// import CreateSearch from "./Search/GetSearch"


const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)

	return (
		<>
			<h2>SEARCH SMTHNG</h2>
		</>
	)
}

export default Home
